============
Installation
============

At the command line::

    $ easy_install wagtail-seopage

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv wagtail-seopage
    $ pip install wagtail-seopage
